# 포인트 정책

## 규칙
- **화폐 단위**: 1원 = 1P. 결제는 포인트로만 가능.
- **잔액 원천**: `POINT.balance`가 유일한 원천. 이력 합산으로 잔액을 계산하지 않는다.
- **충전**:
  - 충전 금액은 **양의 정수만** 허용(0·음수 거부 → `INVALID_AMOUNT`).
  - **상한 없음**, 1회 충전 한도 없음. (검증 최소 정책)
- **차감(주문 결제)**:
  - 잔액이 결제금액 이상일 때만 차감. 부족하면 `INSUFFICIENT_POINT`(주문 실패, 차감 없음).
  - 차감액 = `unit_price(스냅샷) * quantity`.
- **이력**: 모든 증감은 `POINT_HISTORY`에 append-only 기록(`CHARGE`/`USE`), 잔액 변경과 **동일 트랜잭션**.
- **동시성**: 동일 회원 동시 충전·주문 시 잔액 정합성은 `POINT` 행 비관적 락으로 보장. → [ADR-001](../adr/ADR-001-포인트-동시성제어.md)

## 근거 / 배경
- 발제가 정책을 명시하지 않았고, 채점 핵심은 동시성·일관성이므로 **검증 규칙은 최소화**하고 정합성 보장에 집중한다(사용자 선택).
- 상한·한도 등은 과제 범위 밖으로 두되, 필요 시 이 문서만 고치면 확장된다. → [open-questions](../open-questions.md)

## 적용 대상
- point/charge, order/create.
